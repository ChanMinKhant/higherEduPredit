write backend => {
    add predict data,
    crud,
    Admin control, 
    user crud,
    model difficulty,
    score base (20, 40, 100, etc)
    
}

{
    refine retrain for regression,
    refine nav bar, Done
    protect route, Done
    show error message, 
    refine user page,
    admin can watch user's prediction,
    retrain also for higher predication,
    recalculate data,
    add logout, 
    show loading, 
}