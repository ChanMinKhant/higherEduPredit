write backend => {
    add predict data,
    crud,
    Admin control, 
    user crud,
    model difficulty,
    score base (20, 40, 100, etc)
    
}

{
    refine retrain for regression, Discard 
    refine nav bar, Done
    protect route, Done
    show error message, Done
    refine user page, Done
    admin can watch user's prediction, Done
    retrain also for higher predication,Done
    recalculate data, 
    add logout, Done
    show loading, 
    add threadhole 
}